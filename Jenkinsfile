pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"' + env.action + env.merged
                sh '''

                '''
            }
        }
    }
}